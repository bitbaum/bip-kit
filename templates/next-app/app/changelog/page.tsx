/**
 * Example changelog page — ChangelogEntry[] newest first.
 */
export default function ChangelogPage() {
  return (
    <main>
      <h1>Changelog</h1>
      <p>User-facing entries only — not a git log.</p>
    </main>
  );
}
