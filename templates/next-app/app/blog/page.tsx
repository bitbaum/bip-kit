/**
 * Example blog index — replace with your design system.
 * Data: load markdown from content/blog (your choice of FS helper).
 */
export default function BlogIndexPage() {
  return (
    <main>
      <h1>Blog</h1>
      <p>Wire list posts here. Body rendering uses parseContentBlocks from bip-kit.</p>
    </main>
  );
}
